import 'dart:ui';

class Strings {
  static const kBackground = Color(0xff0a1126);
  static const kTransitionDuration = Duration(milliseconds: 350);
  static const kLibreBaskerville = 'LibreBaskerville';
}
